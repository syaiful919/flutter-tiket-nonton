part of 'pages.dart';

class ExploreMoviePage extends StatelessWidget {
  final String genre;

  const ExploreMoviePage({Key key, this.genre}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
